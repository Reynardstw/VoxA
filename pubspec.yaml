name: aol_se
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  translator: ^1.0.3+1
  file_picker: ^10.1.9
  flutter_tts: ^4.0.2
  speech_to_text: ^7.0.0
  http: ^1.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
